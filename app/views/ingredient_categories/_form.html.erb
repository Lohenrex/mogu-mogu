<%= form_with(model: ingredient_category) do |form| %>
  <% if ingredient_category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(ingredient_category.errors.count, "error") %> prohibited this ingredient_category from being saved:</h2>

      <ul>
        <% ingredient_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field is-horizontal">
    <div class="field-label is-normal">
      <label class="label">Name</label>
    </div>
    <div class="field-body">
      <div class="field">
        <p class="control">
          <%= form.text_field :name, autofocus: true, class: "input" %>
        </p>
      </div>
    </div>
  </div>

  <div class="actions has-text-right">
    <%= link_to "Back to ingredient categories", ingredient_categories_path, class: "button is-link" %>
    <%= form.submit "Update", class: "button is-primary is-outlined" %>
  </div>
<% end %>