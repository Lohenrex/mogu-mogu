<%= form_with(model: recipe_category) do |form| %>
  <% if recipe_category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(recipe_category.errors.count, "error") %> prohibited this recipe_category from being saved:</h2>

      <ul>
        <% recipe_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name , class: "label" %>
    <div class="control">
      <%= form.text_field :name, autofocus: true, autocomplete: "name", class: "input is-medium" %>
    </div>
  </div>

  <div class="actions has-text-right">
    <%= link_to "Back to recipe categories", recipe_categories_path, class: "button is-link" %>
    <%= form.submit "Update", class: "button is-primary is-outlined" %>
  </div>
<% end %>